# Everything needed to train, evaluate and run the notebooks.
# The deployed app does not need any of this, and installing it on Streamlit
# Community Cloud wastes build time and memory.
#
#   pip install -r requirements-dev.txt

-r requirements.txt

# Training
albumentations>=1.3.1        # joint image/mask augmentation for segmentation
scikit-learn>=1.4.0          # class weights, stratified splits, metrics
timm>=0.9.12                 # encoder weights behind segmentation-models-pytorch
tqdm>=4.66.0

# Notebooks and analysis
pandas>=2.0.0
seaborn>=0.13.0
tensorboard>=2.15.0          # reading training curves in notebook 03
jupyter>=1.0.0
nbformat>=5.10.0
nbconvert>=7.0.0
